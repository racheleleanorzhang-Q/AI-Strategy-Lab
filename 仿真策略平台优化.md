# 🧠 仿真策略平台优化 — 执行计划

> 目标：从规则引擎升级到可插拔的 ML/DL 模型系统
> 创建时间：2026-05-07
> 状态：全部步骤完成，可进入下一轮迭代

---

## 架构升级方向

```
当前 (规则引擎)                    目标 (可插拔模型系统)
─────────────────                ─────────────────────────
策略参数 ──→ 规则计算 ──→ 结果    策略参数 ──→ [模型选择] ──→ 结果
                                     ├─ 规则引擎 (默认)
                                     ├─ 线性回归
                                     ├─ 随机森林
                                     ├─ LSTM 时序网络
                                     └─ Transformer 多变量
```

---

## 执行步骤

### Step 1: 重构现有代码，解耦引擎和 UI ✅ 已完成

**目标**：将 `simulateStrategy` 从 `StrategySimulator.tsx` 中抽离到独立模块，定义 TypeScript 接口。

**具体工作**：
1. ✅ 创建 `lib/engine/types.ts` — 定义 SimulationParams、DailyResult、SimulationResult、ModelInfo、IEngine 接口
2. ✅ 创建 `lib/engine/ruleEngine.ts` — 迁移现有 simulateStrategy 逻辑，实现 IEngine 接口
3. ✅ 创建 `lib/engine/index.ts` — 引擎注册中心 (registerEngine, getEngine, listEngines, simulate, explain)
4. ✅ 修改 `components/StrategySimulator.tsx` — 移除 145 行内联引擎代码，改为调用 `@/lib/engine`
5. ✅ 验证功能不变 (commit: 849a965)

**验收标准**：
- [x] 页面功能与重构前完全一致
- [x] 引擎代码独立可测试
- [x] TypeScript 类型完整

---

### Step 2: 搭建 Python 后端骨架 ✅ 已完成

**目标**：用 FastAPI 搭建后端服务，实现 BaseModel 抽象类和 RuleEngineModel。

**具体工作**：
1. ✅ 创建 `backend/` 目录结构 (models/, api/, tests/, lib/)
2. ✅ 实现 `backend/models/base.py` — BaseModel ABC + 统一类型 (SimulationParams, SimulationResult, DailyResult, ModelInfo)
3. ✅ 实现 `backend/models/rule_engine.py` — 规则引擎 Python 版 (与前端逻辑一致)
4. ✅ 实现 `backend/models/registry.py` — 模型注册中心 (register_model, get_model, list_models, simulate, explain)
5. ✅ 实现 `backend/api/routes.py` — FastAPI 路由 (/api/models, /api/simulate, /api/health)
6. ✅ 实现 `backend/main.py` — FastAPI 应用入口 + CORS 配置
7. ✅ 实现 `backend/requirements.txt` + `backend/tests/test_engine.py`
8. ✅ API 测试通过 (端口 8101):
   - GET /api/health → {"status":"ok","models":1}
   - GET /api/models → [规则引擎元信息]
   - POST /api/simulate → 模拟结果 (收入 ¥34,635.67, 利润 ¥33,004.43, 留存 78.5%)

**验收标准**：
- [x] FastAPI 服务可启动
- [x] `/api/models` 返回模型列表
- [x] `/api/simulate` 返回与前端一致的模拟结果
- [x] 前端可切换调用后端 API (待 Step 3 集成)

---

### Step 3: 数据生成 + 线性回归 + 模型选择器 ✅ 已完成

**目标**：建立合成数据管道，训练第一个 ML 模型，前端支持模型选择。

**具体工作**：
1. ✅ 实现 `backend/lib/data_generator.py` — 用规则引擎批量生成训练数据（含 save/load）
2. ✅ 实现 `backend/models/linear_model.py` — 线性回归模型（4 个目标变量: profit/retention/revenue/cost）
3. ✅ 实现 `backend/train.py` — 训练脚本（支持 --data-only 和 --n 参数）
4. ✅ 注册线性回归模型到 registry（`backend/models/registry.py`）
5. ✅ 前端添加 `ModelSelector` 组件（`components/ModelSelector.tsx`）
6. ✅ 前端参数台添加模型选择卡片 + 后端 API 调用（`lib/engine/apiClient.ts`）
7. ✅ 添加模型加载 loading 状态（按钮 + 图表 overlay + 状态指示条）

**验收标准**：
- [x] 可生成 1000+ 条合成训练数据（100 条 ≈ 5s）
- [x] 线性回归模型可训练并加载（4 个 Ridge 回归模型，权重已保存）
- [x] 前端可选择"规则引擎"或"线性回归"
- [x] 两种模型结果可对比（规则引擎: 利润 ¥33,004 / 留存 78.5% vs 线性回归: 利润 ¥30,396 / 留存 63.2%）

---

### Step 4: 逐步加模型

**目标**：依次加入随机森林、LSTM、Transformer 模型。

**Phase 4.1 — 随机森林**：
- ~~实现 `backend/models/tree_models.py`~~ ✅ 已完成
- ~~添加特征重要性解释~~ ✅ 已完成

**Phase 4.2 — LSTM (聚合模式)** ✅ 已完成 (2026-05-08)：
- ~~实现 `backend/models/lstm_model.py`~~ ✅ 已完成
- ~~聚合模式（按用户分群预测，非个体）~~ ✅ 已完成
- ~~添加时序数据生成~~ ✅ 已完成
- ~~修复 `lstm_model.py` 语法错误 (缺少逗号 + blended_cost)~~
- ~~训练模型 (2000 条时序数据, 50 epochs, val_loss=0.008157)~~
- ~~API 验证通过 (利润 ¥31,859 / 留存 85.5%)~~

**Phase 4.3 — Transformer** ✅ 已完成 (2026-05-08)：
- ~~实现 `backend/models/transformer_model.py`~~ ✅ 已完成
- ~~策略-用户交互建模~~ ✅ 已完成（自注意力 + 梯度重要性分析）
- ~~网络效应模拟~~ ✅ 已完成（参数交互建模）
- ~~API 验证通过 (利润 ¥32,705 / 留存 73.1%)~~

**验收标准**：
- [x] 所有模型可在前端选择 (规则引擎/线性回归/随机森林/LSTM/Transformer)
- [x] 模型解释层展示各模型的推理依据（规则引擎可解释 / 线性回归权重 / 随机森林特征重要性 / LSTM 时序趋势 / Transformer 注意力分析）
- [x] 深度学习模型推理延迟 < 5s（LSTM ~2s, Transformer ~3s）

---

### Step 5: 模型对比视图 + 精度评估 ✅ 已完成 (2026-05-08)

**目标**：并排展示不同模型的结果差异，建立模型评估体系。

**具体工作**：
1. ✅ 后端新增 `/api/compare` — 多模型对比 API（同一参数多模型并行运行）
2. ✅ 后端新增 `/api/evaluate` — 精度评估 API（MAE/RMSE/MAPE vs 规则引擎基准）
3. ✅ 后端新增 `/api/models/{model_id}/info` — 模型版本信息 API（权重文件/训练日期/数据量/Val Loss）
4. ✅ 前端 `apiClient.ts` 新增类型和函数（ApiCompareResult, ModelEvaluation, compareModels, evaluateModels, fetchModelDetail）
5. ✅ 前端新增第 4 个 Tab "模型对比"，含三个子区域：
   - **多模型并排对比**：选择 2+ 模型 → 点击"开始对比" → 并排展示利润/留存/收入/成本 + 累计利润趋势图
   - **精度评估**：点击"运行评估" → 表格展示各模型 MAE/RMSE/MAPE，最优模型高亮
   - **模型版本信息**：点击"加载版本信息" → 表格展示权重文件/大小/训练日期/数据量/Val Loss

**验收标准**：
- [x] 可同时展示 2-3 个模型的模拟结果
- [x] 模型精度报告可生成（MAE/RMSE/MAPE）
- [x] 模型版本可追溯（权重文件/训练日期/数据量/Val Loss）

---

## 关键设计决策

| 决策 | 方案 | 理由 |
|------|------|------|
| 规则引擎是否保留？ | 保留为默认 | 快速、透明、可解释 |
| 训练数据来源？ | 先用规则引擎生成合成数据 | 冷启动方案 |
| 前端还是后端跑模型？ | 后端 FastAPI | 浏览器跑深度学习不现实 |
| 模型切换后怎么对比？ | 同一参数多模型跑，并排展示 | 体现预测差异 |
| Phase 1/2 部署？ | Next.js API Routes 单部署 | 零额外成本 |
| Phase 3+ 部署？ | FastAPI on Modal/Render | GPU 需求 |

---

## 进度追踪

- [x] Step 1: 解耦引擎和 UI — 已完成 (2026-05-07)
- [x] Step 2: Python 后端骨架 — 已完成 (2026-05-07)
- [x] Step 3: 数据生成 + 线性回归 — 已完成 (2026-05-07)
- [x] Step 4.1: 随机森林 — 已完成
- [x] Step 4.2: LSTM 时序模型 — 已完成 (2026-05-08)
- [x] Step 4.3: Transformer — 已完成 (2026-05-08)
- [x] Step 5: 模型对比 + 精度评估 — 已完成 (2026-05-08)
